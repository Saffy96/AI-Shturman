import { AlertTriangle, LoaderCircle, Navigation } from "lucide-react";
export function NavigationCard({ state, onRetry }: { state: "empty" | "loading" | "error"; onRetry?: () => void }) { const content = state === "loading" ? [<LoaderCircle className="animate-spin" />, "Прокладываем маршрут…"] : state === "error" ? [<AlertTriangle />, "Не удалось построить маршрут"] : [<Navigation />, "Выберите маршрут"]; return <div className="rounded-2xl border border-white/[.08] bg-[#10151d]/90 p-4 text-white backdrop-blur-xl"><div className="flex items-center gap-3 font-black">{content[0]}{content[1]}</div>{state === "loading" ? <div className="mt-3 h-1.5 overflow-hidden rounded-full bg-white/10"><div className="h-full w-2/3 animate-pulse rounded-full bg-[#00e676]" /></div> : null}{state === "error" ? <button onClick={onRetry} className="mt-3 min-h-12 w-full rounded-xl bg-[#ff5252] font-black">Попробовать снова</button> : null}</div>; }
